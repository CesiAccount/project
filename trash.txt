function getconnect()
{
    $host="localhost"; //Serveur ou dans notre cas localhost
    $user="root";  //Utilisateur par défaut
    $password="";  //Mot de passs par défaut
    $db="erin1"; //Nom de la base de donnée

    $data= new mysqli($host,$user,$password,$db,3307) or die("not connect"); //Connection à la base de données
    echo "Connect";
}
?>
<?php